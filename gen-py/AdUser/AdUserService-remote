#!/usr/bin/env python
#
# Autogenerated by Thrift Compiler (0.8.0)
#
# DO NOT EDIT UNLESS YOU ARE SURE THAT YOU KNOW WHAT YOU ARE DOING
#
#  options string: py
#

import sys
import pprint
from urlparse import urlparse
from thrift.transport import TTransport
from thrift.transport import TSocket
from thrift.transport import THttpClient
from thrift.protocol import TBinaryProtocol

import AdUserService
from ttypes import *

if len(sys.argv) <= 1 or sys.argv[1] == '--help':
  print ''
  print 'Usage: ' + sys.argv[0] + ' [-h host[:port]] [-u url] [-f[ramed]] function [arg1 [arg2...]]'
  print ''
  print 'Functions:'
  print '  i32 addAdUser(AdUser adUser)'
  print '  i32 updateAdUserInfo(AdUser adUser)'
  print '  i32 delAdUserById(i32 uid)'
  print '  AdUser getAdUserById(i32 uid)'
  print '  i32 updateAdUserStatus(i32 uid, AccountStatus status)'
  print '   getAdUsersByCid(i32 creator_id, AccountStatus account_status)'
  print '  AdUser getAdUserByName(string user_name)'
  print ''
  sys.exit(0)

pp = pprint.PrettyPrinter(indent = 2)
host = 'localhost'
port = 9090
uri = ''
framed = False
http = False
argi = 1

if sys.argv[argi] == '-h':
  parts = sys.argv[argi+1].split(':')
  host = parts[0]
  if len(parts) > 1:
    port = int(parts[1])
  argi += 2

if sys.argv[argi] == '-u':
  url = urlparse(sys.argv[argi+1])
  parts = url[1].split(':')
  host = parts[0]
  if len(parts) > 1:
    port = int(parts[1])
  else:
    port = 80
  uri = url[2]
  if url[4]:
    uri += '?%s' % url[4]
  http = True
  argi += 2

if sys.argv[argi] == '-f' or sys.argv[argi] == '-framed':
  framed = True
  argi += 1

cmd = sys.argv[argi]
args = sys.argv[argi+1:]

if http:
  transport = THttpClient.THttpClient(host, port, uri)
else:
  socket = TSocket.TSocket(host, port)
  if framed:
    transport = TTransport.TFramedTransport(socket)
  else:
    transport = TTransport.TBufferedTransport(socket)
protocol = TBinaryProtocol.TBinaryProtocol(transport)
client = AdUserService.Client(protocol)
transport.open()

if cmd == 'addAdUser':
  if len(args) != 1:
    print 'addAdUser requires 1 args'
    sys.exit(1)
  pp.pprint(client.addAdUser(eval(args[0]),))

elif cmd == 'updateAdUserInfo':
  if len(args) != 1:
    print 'updateAdUserInfo requires 1 args'
    sys.exit(1)
  pp.pprint(client.updateAdUserInfo(eval(args[0]),))

elif cmd == 'delAdUserById':
  if len(args) != 1:
    print 'delAdUserById requires 1 args'
    sys.exit(1)
  pp.pprint(client.delAdUserById(eval(args[0]),))

elif cmd == 'getAdUserById':
  if len(args) != 1:
    print 'getAdUserById requires 1 args'
    sys.exit(1)
  pp.pprint(client.getAdUserById(eval(args[0]),))

elif cmd == 'updateAdUserStatus':
  if len(args) != 2:
    print 'updateAdUserStatus requires 2 args'
    sys.exit(1)
  pp.pprint(client.updateAdUserStatus(eval(args[0]),eval(args[1]),))

elif cmd == 'getAdUsersByCid':
  if len(args) != 2:
    print 'getAdUsersByCid requires 2 args'
    sys.exit(1)
  pp.pprint(client.getAdUsersByCid(eval(args[0]),eval(args[1]),))

elif cmd == 'getAdUserByName':
  if len(args) != 1:
    print 'getAdUserByName requires 1 args'
    sys.exit(1)
  pp.pprint(client.getAdUserByName(args[0],))

else:
  print 'Unrecognized method %s' % cmd
  sys.exit(1)

transport.close()
