#!/usr/bin/env python
#
# Autogenerated by Thrift Compiler (0.8.0)
#
# DO NOT EDIT UNLESS YOU ARE SURE THAT YOU KNOW WHAT YOU ARE DOING
#
#  options string: py
#

import sys
import pprint
from urlparse import urlparse
from thrift.transport import TTransport
from thrift.transport import TSocket
from thrift.transport import THttpClient
from thrift.protocol import TBinaryProtocol

import MediaService
from ttypes import *

if len(sys.argv) <= 1 or sys.argv[1] == '--help':
  print ''
  print 'Usage: ' + sys.argv[0] + ' [-h host[:port]] [-u url] [-f[ramed]] function [arg1 [arg2...]]'
  print ''
  print 'Functions:'
  print '   getAllMediaInfo()'
  print '   getAllFirstMediaInfo()'
  print '   getAllSecondMediaInfo()'
  print '  Media getMediaInfoById(i64 id)'
  print '   getAllSecondMediaInfoByFirstId(i64 id)'
  print '   getAllHostInfo()'
  print '  Host getHostInfoById(i64 id)'
  print '   getAllAdplaceInfo()'
  print '   getAllInfo()'
  print ''
  sys.exit(0)

pp = pprint.PrettyPrinter(indent = 2)
host = 'localhost'
port = 9090
uri = ''
framed = False
http = False
argi = 1

if sys.argv[argi] == '-h':
  parts = sys.argv[argi+1].split(':')
  host = parts[0]
  if len(parts) > 1:
    port = int(parts[1])
  argi += 2

if sys.argv[argi] == '-u':
  url = urlparse(sys.argv[argi+1])
  parts = url[1].split(':')
  host = parts[0]
  if len(parts) > 1:
    port = int(parts[1])
  else:
    port = 80
  uri = url[2]
  if url[4]:
    uri += '?%s' % url[4]
  http = True
  argi += 2

if sys.argv[argi] == '-f' or sys.argv[argi] == '-framed':
  framed = True
  argi += 1

cmd = sys.argv[argi]
args = sys.argv[argi+1:]

if http:
  transport = THttpClient.THttpClient(host, port, uri)
else:
  socket = TSocket.TSocket(host, port)
  if framed:
    transport = TTransport.TFramedTransport(socket)
  else:
    transport = TTransport.TBufferedTransport(socket)
protocol = TBinaryProtocol.TBinaryProtocol(transport)
client = MediaService.Client(protocol)
transport.open()

if cmd == 'getAllMediaInfo':
  if len(args) != 0:
    print 'getAllMediaInfo requires 0 args'
    sys.exit(1)
  pp.pprint(client.getAllMediaInfo())

elif cmd == 'getAllFirstMediaInfo':
  if len(args) != 0:
    print 'getAllFirstMediaInfo requires 0 args'
    sys.exit(1)
  pp.pprint(client.getAllFirstMediaInfo())

elif cmd == 'getAllSecondMediaInfo':
  if len(args) != 0:
    print 'getAllSecondMediaInfo requires 0 args'
    sys.exit(1)
  pp.pprint(client.getAllSecondMediaInfo())

elif cmd == 'getMediaInfoById':
  if len(args) != 1:
    print 'getMediaInfoById requires 1 args'
    sys.exit(1)
  pp.pprint(client.getMediaInfoById(eval(args[0]),))

elif cmd == 'getAllSecondMediaInfoByFirstId':
  if len(args) != 1:
    print 'getAllSecondMediaInfoByFirstId requires 1 args'
    sys.exit(1)
  pp.pprint(client.getAllSecondMediaInfoByFirstId(eval(args[0]),))

elif cmd == 'getAllHostInfo':
  if len(args) != 0:
    print 'getAllHostInfo requires 0 args'
    sys.exit(1)
  pp.pprint(client.getAllHostInfo())

elif cmd == 'getHostInfoById':
  if len(args) != 1:
    print 'getHostInfoById requires 1 args'
    sys.exit(1)
  pp.pprint(client.getHostInfoById(eval(args[0]),))

elif cmd == 'getAllAdplaceInfo':
  if len(args) != 0:
    print 'getAllAdplaceInfo requires 0 args'
    sys.exit(1)
  pp.pprint(client.getAllAdplaceInfo())

elif cmd == 'getAllInfo':
  if len(args) != 0:
    print 'getAllInfo requires 0 args'
    sys.exit(1)
  pp.pprint(client.getAllInfo())

else:
  print 'Unrecognized method %s' % cmd
  sys.exit(1)

transport.close()
